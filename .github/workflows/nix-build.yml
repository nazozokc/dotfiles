name: "CI: Nix build check"
on:
  pull_request:
    paths:
      - "nix/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/nix-build.yml"
  push:
    branches: [main]
    paths:
      - "nix/**"
      - "flake.nix"
      - "flake.lock"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ${{ matrix.os }}  # ← ここに移動
    timeout-minutes: 15
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: homeConfigurations.nazozokc.activationPackage
          - os: macos-latest
            target: darwinConfigurations.nazozokc.system
            # ↑ ここから runs-on を削除
    steps:
      - uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v21
      - uses: DeterminateSystems/magic-nix-cache-action@v5
      - name: nix flake check
        run: nix flake check --no-build
      - name: Build config (dry-run)
        run: nix build .#${{ matrix.target }} --dry-run
